<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gochuchamchi.mapper.NoticeMapper">

  <resultMap id="noticeResultMap" type="NoticeDto">
    <id property="id" column="id"/>
    <result property="title" column="title"/>
    <result property="content" column="content"/>
    <result property="author" column="author"/>
    <result property="pinned" column="pinned"/>
    <result property="views" column="views"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <select id="findAll" resultMap="noticeResultMap">
    SELECT * FROM notices
    <where>
      <if test="keyword != null and keyword != ''">
        <choose>
          <when test="type == 'title'">AND title LIKE CONCAT('%',#{keyword},'%')</when>
          <when test="type == 'content'">AND content LIKE CONCAT('%',#{keyword},'%')</when>
          <otherwise>AND (title LIKE CONCAT('%',#{keyword},'%') OR content LIKE CONCAT('%',#{keyword},'%'))</otherwise>
        </choose>
      </if>
      <if test="period == 'week'">AND created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)</if>
      <if test="period == 'month'">AND created_at >= DATE_SUB(NOW(), INTERVAL 1 MONTH)</if>
      <if test="period == 'year'">AND created_at >= DATE_SUB(NOW(), INTERVAL 1 YEAR)</if>
    </where>
    ORDER BY pinned DESC, created_at DESC
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <select id="findById" resultMap="noticeResultMap">
    SELECT * FROM notices WHERE id = #{id}
  </select>

  <select id="countAll" resultType="int">
    SELECT COUNT(*) FROM notices
    <where>
      <if test="keyword != null and keyword != ''">
        <choose>
          <when test="type == 'title'">AND title LIKE CONCAT('%',#{keyword},'%')</when>
          <when test="type == 'content'">AND content LIKE CONCAT('%',#{keyword},'%')</when>
          <otherwise>AND (title LIKE CONCAT('%',#{keyword},'%') OR content LIKE CONCAT('%',#{keyword},'%'))</otherwise>
        </choose>
      </if>
      <if test="period == 'week'">AND created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)</if>
      <if test="period == 'month'">AND created_at >= DATE_SUB(NOW(), INTERVAL 1 MONTH)</if>
      <if test="period == 'year'">AND created_at >= DATE_SUB(NOW(), INTERVAL 1 YEAR)</if>
    </where>
  </select>

  <update id="incrementViews">
    UPDATE notices SET views = views + 1 WHERE id = #{id}
  </update>

  <!-- 관리자용 -->
  <select id="findAllForAdmin" resultMap="noticeResultMap">
    SELECT * FROM notices ORDER BY pinned DESC, created_at DESC
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO notices (title, content, author, pinned, created_at)
    VALUES (#{title}, #{content}, #{author}, #{pinned}, NOW())
  </insert>

  <update id="update">
    UPDATE notices SET title=#{title}, content=#{content}, pinned=#{pinned} WHERE id=#{id}
  </update>

  <delete id="delete">
    DELETE FROM notices WHERE id=#{id}
  </delete>

</mapper>
