<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gochuchamchi.mapper.ProductSizeMapper">

  <insert id="insertSizes">
    INSERT INTO product_sizes (product_id, size_name, stock, sort_order, sold_out)
    VALUES
    <foreach collection="sizes" item="s" separator=",">
      (#{s.productId}, #{s.sizeName}, #{s.stock}, #{s.sortOrder}, 0)
    </foreach>
  </insert>

  <select id="findByProductId" resultType="ProductSizeDto">
    SELECT id, product_id as productId, size_name as sizeName,
           stock, sort_order as sortOrder, sold_out as soldOut
    FROM product_sizes
    WHERE product_id = #{productId}
    ORDER BY sort_order
  </select>

</mapper>